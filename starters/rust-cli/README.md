# PicoVolt Rust starter

The PicoVolt crate is pinned to the release verified with this starter.

```sh
cargo run
```

The starter is safe to run repeatedly. Each execution appends one row to its
development workspace through a reusable prepared statement.
